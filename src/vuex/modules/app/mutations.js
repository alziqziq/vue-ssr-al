export const setAuthor = (state, author) => {
  state.app.author = author;
};

export const setDescription = (state, value) => {
  state.app.description = value;
};

export const setMsg = (state, value) => {
  state.app.msg = value;
};